"use stricts"; // Treats all js code as a newer version...
// alert(3 + 3) This will throw an Error, Because we are using Node.js Not Browser...


console.log( 3 
    + 3) // Code readability should be high
console.log("Durgesh")

let name = "Durgesh" // String --> ""
let age = 18 // Number --> 1 2 3...1000..
let loggedIn = true // Boolean --> true/false
let temprature = null // Datatype --> Object
let value; // Datatype --> undefined
let range = 99999999n // bigInt --> 999999n / 15 or more time number(999999999999999)

console.table([typeof(name), typeof(age), typeof(loggedIn), typeof(temprature), typeof(value), typeof(range)])

